<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:windowTranslucentStatus">true</item>

        <item name="android:textColorPrimary">@color/textColorPrimary</item>
        <item name="android:textColorSecondary">@color/textColorSecondary</item>

        <item name="android:textColorPrimaryInverse">@color/textColorPrimaryInverted</item>
        <item name="android:textColorSecondaryInverse">@color/textColorSecondaryInverted</item>
    </style>

    <style name="AppThemeGradient" parent="AppTheme">
        <item name="android:windowBackground">@drawable/gradient_background</item>

        <item name="android:textColorPrimary">@color/textColorPrimaryInverted</item>
        <item name="android:textColorSecondary">@color/textColorSecondaryInverted</item>
        <item name="android:textColorHint">@color/textColorSecondaryInverted</item>

        <item name="colorAccent">@color/primaryWhite</item>
    </style>

    <style name="SplashTheme" parent="AppThemeGradient">
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>

</resources>
